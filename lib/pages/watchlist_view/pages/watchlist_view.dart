import 'package:flutter/cupertino.dart';

class watchlist_view extends StatelessWidget {
  static const String routeName = "watchlist_view";

  const watchlist_view({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("watchlist_view"),
    );
  }
}