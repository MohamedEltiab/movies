import 'package:flutter/cupertino.dart';

class browser_view extends StatelessWidget {
  static const String routeName = "browser_view";
  const browser_view({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("browser_view"),
    );
  }
}
